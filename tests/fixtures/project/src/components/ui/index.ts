// The component barrel the skill tells an agent to scan before building anything new.
// A stub: the tests only assert that `components.index` resolves to a real file.
export const components = ['AppButton', 'AppCheckbox', 'AppText'] as const;
